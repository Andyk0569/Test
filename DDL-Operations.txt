
--create
CREATE TABLE EMPLOYEES (
    EMP_ID NUMBER PRIMARY KEY,
    EMP_NAME VARCHAR2(50) NOT NULL,
    DEPT_ID NUMBER,
    SALARY NUMBER CHECK (SALARY > 0)
);
INSERT INTO EMPLOYEES (EMP_ID, EMP_NAME, DEPT_ID, SALARY) VALUES (1, 'John Doe', 101, 5000);
INSERT INTO EMPLOYEES (EMP_ID, EMP_NAME, DEPT_ID, SALARY) VALUES (2, 'Jane Smith', 102, 5500);
INSERT INTO EMPLOYEES (EMP_ID, EMP_NAME, DEPT_ID, SALARY) VALUES (3, 'Sam Brown', 103, 6000);
COMMIT;

--Alter
ALTER TABLE EMPLOYEES
ADD EMAIL VARCHAR2(50);

--Drop
ALTER TABLE EMPLOYEES
DROP COLUMN EMAIL;
select * from EMPLOYEES;

CREATE TABLE EMPLOYEE (
    EMP_ID NUMBER PRIMARY KEY,
    EMP_NAME VARCHAR2(50) NOT NULL,
    DEPT_ID NUMBER,
    SALARY NUMBER CHECK (SALARY > 0)
);
INSERT INTO EMPLOYEE (EMP_ID, EMP_NAME, DEPT_ID, SALARY) VALUES (1, 'John Doe',201, 5000);
INSERT INTO EMPLOYEE (EMP_ID, EMP_NAME, DEPT_ID, SALARY) VALUES (2, 'Jane Smith',202, 5500);
INSERT INTO EMPLOYEE (EMP_ID, EMP_NAME, DEPT_ID, SALARY) VALUES (3, 'Sam Brown', 203,6000);
COMMIT;


--Truncate
TRUNCATE TABLE EMPLOYEE;

select * from EMPLOYEE;
